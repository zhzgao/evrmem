# QMD Vector Memory System
# QMD 向量记忆系统依赖

# 核心依赖
chromadb>=0.4.0
sentence-transformers>=2.2.0

# NumPy 版本限制（兼容性问题）
# 注意：NumPy 2.x 与某些依赖有兼容性问题，建议先用 NumPy 1.x
numpy<2

# 配置解析
pyyaml>=6.0

# 可选依赖（用于增强功能）
# gradio>=3.0  # Web UI 界面
# rich>=13.0   # 富文本输出
